"use client";
import PageContainer from "@/components/ui/PageContainer";

export default function StoragePage() {
  return (
    <PageContainer title="Storage" subtitle="Manage persisted artifacts and snapshots.">
      <p className="text-sm text-[var(--foreground-muted)]">Storage features coming soon...</p>
    </PageContainer>
  );
}
