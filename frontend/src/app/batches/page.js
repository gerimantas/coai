"use client";
import PageContainer from "@/components/ui/PageContainer";

export default function BatchesPage() {
  return (
    <PageContainer title="Batches" subtitle="Large offline tasks and exports.">
      <p className="text-sm text-[var(--foreground-muted)]">Batch features coming soon...</p>
    </PageContainer>
  );
}
