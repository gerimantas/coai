"use client";
import PageContainer from "@/components/ui/PageContainer";

export default function ImagesPage() {
  return (
    <PageContainer title="Images" subtitle="Generate, view, and manage images.">
      <p className="text-sm text-[var(--foreground-muted)]">Images features coming soon...</p>
    </PageContainer>
  );
}
