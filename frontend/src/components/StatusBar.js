"use client";
export default function StatusBar() {
  return (
    <footer className="border-t border-default px-4 py-2 text-sm shadow-inner" style={{backgroundColor: 'var(--background-light)', color: 'var(--foreground-muted)'}}>
      Status: Ready
    </footer>
  );
}
